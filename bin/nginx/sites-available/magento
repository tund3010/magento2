# sudo cp bin/nginx/sites-available/magento /etc/nginx/sites-available/
# sudo ln -s /etc/nginx/sites-available/magento /etc/nginx/sites-enabled
# sudo nginx -t
# sudo service nginx restart

# find var vendor pub/static pub/media app/etc -type f -exec sudo chmod g+w {} \;
# find var vendor pub/static pub/media app/etc -type d -exec sudo chmod g+ws {} \;
# sudo chown -R :www-data .

## Example configuration:
upstream fastcgi_backend {
   server   unix:/var/run/php/php8.3-fpm.sock;
}
server {
    listen 8080;
#    server_name mage.dev;
    set $MAGE_ROOT /home/ndt/magento2;
    set $MAGE_DEBUG_SHOW_ARGS 0;

    include /home/ndt/magento2/nginx.conf.sample;
}